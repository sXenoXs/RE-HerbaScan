/// Service for managing application configuration.
/// (Previously held API keys; no live LLM is used—explanations are deterministic from JSON.)
class ConfigService {
  // Reserved for future app-wide config (e.g. feature flags, endpoints) if needed.
}
